# Supabase project (same URL/key across dev, staging, prod — one project, three schemas)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Differentiator between environments. One of: dev | staging | prod
# Local dev -> dev, Vercel Preview -> staging, Vercel Production -> prod
VITE_DB_SCHEMA=dev
